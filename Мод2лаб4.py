rom typing import Any  # Импорт модуля для аннотации типов

class Animal:
    """
    Базовый класс, представляющий животное.
    """
    def __init__(self, name: str, age: int):
        """
        Конструктор класса Animal.

        :param name: Имя животного
        :param age: Возраст животного
        """
        self._name = name  # Инкапсулированный атрибут (непубличный), хранит имя животного
        self.age = age  # Публичный атрибут, хранит возраст животного

    def __str__(self) -> str:
        """
        Возвращает строковое представление объекта для пользователя.
        """
        return f"Животное {self._name}, возраст {self.age} лет"  # Форматированная строка с описанием животного

    def __repr__(self) -> str:
        """
        Возвращает строковое представление объекта для разработчиков.
        """
        return f"Animal(name={self._name!r}, age={self.age})"  # Техническое описание объекта с подробностями

    def make_sound(self) -> str:
        """
        Метод, который должен быть переопределен в дочерних классах.
        Возвращает базовую строку-звук.
        """
        return "Неизвестный звук"  # Стандартное поведение: возвращает строку по умолчанию


class Mammal(Animal):
    """
    Класс, представляющий млекопитающее. Дочерний класс от Animal.
    """
    def __init__(self, name: str, age: int, fur_color: str):
        """
        Конструктор класса Mammal, расширяет конструктор Animal.

        :param name: Имя животного
        :param age: Возраст животного
        :param fur_color: Цвет шерсти млекопитающего
        """
        super().__init__(name, age)  # Вызов конструктора базового класса Animal
        self.fur_color = fur_color  # Атрибут для хранения цвета шерсти

    def __str__(self) -> str:
        """
        Перегруженный метод для более детального описания млекопитающего.
        """
        return f"Млекопитающее {self._name}, возраст {self.age} лет, цвет шерсти: {self.fur_color}"  # Возвращает строку с полным описанием

    def __repr__(self) -> str:
        """
        Перегруженный метод __repr__ для класса Mammal.
        Возвращает строковое представление объекта с учетом атрибутов класса Mammal.
        """
        return f"Mammal(name={self._name!r}, age={self.age}, fur_color={self.fur_color!r})"  # Представление объекта Mammal

    def make_sound(self) -> str:
        """
        Переопределенный метод make_sound для млекопитающих.
        Обоснование: метод переопределен для отображения характерного поведения млекопитающих.
        Каждое млекопитающее может издавать уникальные звуки, отличающиеся от базового класса Animal.
        Это демонстрирует разнообразие звукового поведения в зависимости от вида.
        """
        return "Млекопитающее издает звук"  # Специфическая реализация метода


class Dog(Mammal):
    """
    Класс, представляющий собаку. Дочерний класс от Mammal.
    """
    def __init__(self, name: str, age: int, fur_color: str, breed: str):
        """
        Конструктор класса Dog, расширяет Mammal, добавляя породу собаки.

        :param name: Имя собаки
        :param age: Возраст собаки
        :param fur_color: Цвет шерсти
        :param breed: Порода собаки
        """
        super().__init__(name, age, fur_color)  # Вызов конструктора родительского класса Mammal
        self.breed = breed  # Атрибут для хранения породы собаки

    def __str__(self) -> str:
        """
        Перегруженный метод для описания собаки с указанием породы.
        """
        return f"Собака {self._name}, порода {self.breed}, возраст {self.age} лет, цвет шерсти: {self.fur_color}"  # Возвращает описание собаки

    def __repr__(self) -> str:
        """
        Перегруженный метод __repr__ для класса Dog.
        Возвращает строковое представление объекта с учетом атрибутов класса Dog.
        """
        return (
            f"Dog(name={self._name!r}, age={self.age}, "
            f"fur_color={self.fur_color!r}, breed={self.breed!r})"
        )  # Представление объекта Dog

    def make_sound(self) -> str:
        """
        Перегруженный метод make_sound для конкретного звука собаки.
        Обоснование: метод переопределен для демонстрации характерного поведения собаки.
        Собаки издают уникальные звуки (лай), отличные от других млекопитающих.
        Это подчеркивает уникальность звукового поведения, свойственного каждой породе собаки.
        Например, овчарки лают громко и пронзительно, тогда как мопсы могут издавать более хриплые звуки.
        """
        return "Гав-гав!"  # Возвращает строку, имитирующую лай собаки

    def fetch(self, item: str) -> str:
        """
        Метод для демонстрации поведения собаки (принести предмет).

        :param item: Предмет, который собака должна принести.
        :return: Строка с описанием действия.
        """
        return f"{self._name} принес(ла) {item}!"  # Сообщает о выполнении действия


if __name__ == "__main__":
    """
    Точка входа для выполнения примеров использования классов.
    Это предотвращает выполнение кода при импорте модуля.
    """
    # Пример создания объекта базового класса Animal
    animal = Animal("Неизвестное животное", 5)  # Создание экземпляра Animal с атрибутами

    # Пример создания объекта подкласса Mammal
    mammal = Mammal("Млекопитающее", 3, "Коричневый")  # Экземпляр Mammal с цветом шерсти

    # Пример создания объекта подкласса Dog
    dog = Dog("Барсик", 2, "Черный", "Лабрадор")  # Экземпляр Dog с указанием породы

    # Вывод информации о каждом объекте через метод __str__
    print(animal)  # Вывод описания Animal
    print(mammal)  # Вывод описания Mammal
    print(dog)  # Вывод описания Dog

    # Демонстрация методов make_sound и fetch
    print(animal.make_sound())  # Вызов make_sound() для Animal (общий звук)
    print(mammal.make_sound())  # Вызов make_sound() для Mammal (звук млекопитающего)
    print(dog.make_sound())  # Вызов make_sound() для Dog (гав-гав)
    print(dog.fetch("мяч"))  # Демонстрация метода fetch() для Dog (принесение мяча)
    